import React from "react";
import { Modal, TextField, Box, Button, IconButton } from "@mui/material";

const ModalNewCalendar = ({open, handleClose}) => {
    return (
        <Modal open={open} onClose={handleClose}>
            
        </Modal>
    );
}

export default ModalNewCalendar;